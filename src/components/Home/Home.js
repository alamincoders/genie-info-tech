import React from "react";
import MainHeader from "../MainHeader/MainHeader";

const Home = () => {
  return (
    <section>
      <header>
        <MainHeader />
      </header>
      <section>Home</section>
    </section>
  );
};

export default Home;
